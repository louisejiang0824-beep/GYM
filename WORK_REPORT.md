# “动起来 · 健身记录”收尾检查与整改报告

日期：2026-09-19  
检查视角：iPhone 端用户、首次使用者、连续记录训练的用户  
项目形态：静态 HTML / CSS / JavaScript PWA

## 一、结论摘要

本次收尾检查覆盖了首页、记录训练、力量训练、有氧训练、训练历史、训练汇总、设置、导入导出、草稿恢复、完成反馈和 PWA 图标等主要路径。

当前版本已经达到“可以继续使用和部署”的状态：

- iPhone 15 Pro 参考尺寸 393×852 下没有横向溢出。
- 320×568、393×852、430×932 和 852×393 横屏下，主内容、底部导航都保持在屏幕范围内。
- 直接打开 `#new`、`#summary`、`#settings` 或刷新这些页面时，不会先显示旧页面再跳转。
- 记录训练页面进入时不再出现上下弹动或页面位移。
- 力量训练与有氧训练已经是两条清晰的填写流程。
- 训练草稿会自动保存，离开后再次进入可以恢复。
- “复制上次训练”会优先查找相同训练部位，而不是简单复制最近一条记录。
- 训练完成后会显示轻量完成动画，并自动进入该条记录详情。
- 当前没有服务端账号和云数据库，训练数据仍保存在当前浏览器的 `localStorage` 中；Cloudflare Pages 只负责发布代码，不会自动同步数据。

## 二、本次发现并整改的问题

### 1. 直接打开记录页时可能加载旧版流程

原因是 `app.js` 在自己的脚本结束时立即调用 `render()`，而 `cardio.js` 后续才会覆盖力量/有氧流程相关函数。用户通过链接直达 `#new` 或刷新页面时，可能先执行旧版记录页。

整改：

- 移除 `app.js` 内部的立即渲染。
- 在 `index.html` 的所有功能脚本加载完成后统一调用 `render()`。
- 这样首次渲染一定使用完整模块，避免首屏闪动和功能不一致。

### 2. 有氧训练的图标来源不统一

原有有氧类型使用 emoji、Unicode 符号和不同视觉风格的字符，在 iOS 上容易出现大小、基线和线条不一致。

整改：

- 将跑步、快走、骑行、游泳、跳绳等有氧入口统一改为本地 SVG 线性图标。
- 使用与首页、历史、设置一致的 iOS 风格图标系统。
- 删除、编辑、添加、返回等操作继续使用同一套 SVG 图标，不依赖 emoji 或文本字符。
- 网页/PWA 无法直接加载 Apple 私有 SF Symbols，因此使用本地合法 SVG 作为视觉替代，不混用第三方图标库。

### 3. 汇总日期详情存在图标转义风险

有氧记录在汇总日期详情中先拼接 SVG，再统一进行文本转义，可能将 SVG 当作普通文本显示。

整改：

- 将图标和文字分开渲染。
- 图标直接作为 SVG 节点输出，文字继续进行安全转义。

### 4. 有氧类型容器的语义不准确

原结构使用 `listbox`，但内部实际上是按钮式的互斥选择。

整改：

- 改为普通语义分组 `role="group"`。
- 各个选项保留 `aria-pressed`，与按钮式选择行为一致。

### 5. 部分移动端交互区域偏小

检查发现训练类型按钮高度约 38px，汇总日历格在部分屏幕上约 40px，低于 iOS 更舒适的 44pt 触控目标。

整改：

- 训练类型按钮统一提升到至少 44px 高。
- 汇总日历改为更合理的左右内边距和 4px 网格间距，在 393px 宽度下每个日期格约 44px。
- 保证不引入横向滚动。

### 6. 草稿状态缺少辅助技术播报语义

“已自动保存草稿”和“填写内容会自动保存”属于重要状态反馈，之前仅作为普通文本显示。

整改：

- 为草稿状态增加 `role="status"` 和 `aria-live="polite"`。
- 屏幕阅读器可以在状态变化时正常获知，不打断用户填写。

### 7. 移动端品牌标题字体未完全统一

移动首页的英雄区域仍可能继承桌面品牌字体配置。

整改：

- 移动端英雄标题、英文眉标题和表单眉标题统一使用系统字体栈：

```css
-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", sans-serif
```

## 三、手机用户完整体验检查

### 本轮训练页增量优化

在收尾后又针对训练记录页做了四项小范围优化，没有改变页面整体结构：

- “复制上次动作”现在只复制动作名称，并生成空白待填写组；历史重量和次数仍通过“上次：80 kg × 8 次”提示展示。
- 当用户开始填写下一个动作的重量或次数时，如果前一个动作已经有名称和有效次数，前一个动作会自动折叠。
- 折叠卡片显示动作名称和简要结果，例如“卧推 · 80 kg × 8 × 3组”，点击折叠/展开按钮即可继续修改。
- RIR 从动作表格、填写说明和详情展示中移除；`rir` 字段仍保留在数据结构和旧记录中，避免历史数据损坏。
- “复制上次动作”按钮在 320px、375px、393px 和 430px 宽度下保持单行，未引入横向滚动。

### 首页

- 顶部品牌标志、标题和“已保存至本机”状态清晰可见。
- 首页已改为沉浸式封面：训练氛围图、深绿蒙层和底部渐变自然衔接到内容区，不再使用独立黑色 banner 卡片。
- 封面图已重新设置焦点位置：移动端人物落在封面中右部，标题和按钮保持在左侧可读区域，避免人物被文字遮住。
- 主行动按钮“开始今天训练”处在首屏主要视觉区域，仍然指向原有 `#new` 训练流程。
- 训练统计改为无厚重边框的横向信息带，通过细分隔线保留三项数据层级。
- 最近训练改为轻量分隔线列表，保留日期、部位、时长、动作数以及编辑/删除操作。
- 本轮统一了首页、历史、汇总、设置的页面横向间距，使用全局 `--page-gutter` 控制小屏到桌面端的左右留白。
- 历史记录、汇总统计与日历/周报折叠区、设置折叠区统一改为 List Section：弱分隔线、低阴影、连续信息流，保留原有展开、筛选、编辑、删除和数据管理操作。
- 无训练时显示明确空状态和新建入口。
- 底部导航固定在安全区域内，当前栏目有轻量选中背景。
- 首页已经移除容易造成拥挤的训练编辑图库区域。

### 记录训练

- 进入页面时 `scrollY` 保持为 0，没有上下弹动。
- 页面不使用进入动画推动表单位置，避免用户点击时错位。
- 力量训练：日期、开始/结束时间、部位、动作、组数、重量和次数均可连续填写；RIR 仅在底层兼容旧数据，主界面不展示。
- 有氧训练：日期、时长、运动类型和对应指标独立显示，不再进入力量训练的动作流程。
- 添加动作、添加组、删除动作均有明确的 SVG 图标和点击反馈。
- 训练完成后显示绿色圆环完成动画，随后进入详情页。

### 草稿与恢复

- 表单输入会自动保存。
- 切换页面或刷新后重新进入记录页，可以恢复未完成内容。
- 恢复状态会显示摘要，并提供“清除草稿”入口。

### 历史

- 训练记录按列表展示。
- 编辑、删除按钮拥有 44px 级别的交互区域。
- 有氧记录和力量记录都能进入对应详情。

### 汇总

- 月度统计、周训练总结、肌群有效组数、动作进步和个人纪录使用折叠/分区展示。
- 日历日期可点击查看当天记录。
- 日期格在 iPhone 15 Pro 宽度下不会挤压或溢出。
- 底部导航不会遮挡页面最后内容；滚动到底部后，最后一个内容区域仍能停在导航栏上方。

### 设置

- 身高、体重、体重历史、数据导入导出、清空数据均可访问。
- 体重变化使用单线图展示。
- 导入窗口具备 `role="dialog"`、标题关联和可识别的关闭按钮。
- 危险操作仍保持低频、低存在感，并要求二次确认。

## 四、自动化验证结果

### 尺寸检查

| 尺寸 | 页面 | 结果 |
| --- | --- | --- |
| 320×568 | 首页 / 记录 / 历史 / 汇总 / 设置 | 通过，无横向溢出 |
| 393×852 | iPhone 15 Pro 参考尺寸 | 通过，无横向溢出 |
| 430×932 | 大尺寸 iPhone | 通过，无横向溢出 |
| 852×393 | 横屏参考尺寸 | 通过，无横向溢出 |

### 功能路径

- 直接打开 `#new`：通过。
- 直接打开 `#summary`：通过。
- 直接打开 `#settings`：通过。
- 有氧训练保存：通过，记录类型为 `cardio`。
- 力量训练保存：通过，组、重量和次数数据正确写入。
- 完成动画结束后进入详情：通过。
- 草稿自动保存与恢复：通过。
- 相同部位复制上次训练：通过。
- 导入窗口打开与关闭：通过。
- JavaScript 语法检查：通过。
- `node tools/layout-audit.cjs`：`failures=[]`、`errors=[]`。
- 控制台页面错误：检查路径中未发现。
- `prefers-reduced-motion: reduce`：页面不会残留运行中的进入动画。

## 五、设计过程总结

### 1. 设计定位

“动起来”被定位为一个克制、专业、适合长期记录的训练日志，而不是复杂的健身数据后台。设计重点依次是：

1. 让用户快速开始一次训练。
2. 让组数和动作填写过程清楚、稳定、不跳动。
3. 让历史记录和趋势数据容易回看。
4. 让设置和数据迁移不干扰主要训练流程。

### 2. 视觉系统

- 深绿色作为品牌主色和主要行动色。
- 浅灰绿色作为页面背景，降低长时间使用的视觉压力。
- 白色/浅色卡片承载信息，边框和阴影保持轻量。
- 统一 8px 间距体系，并在移动端通过 12、16、20、24px 形成层级。
- 圆角主要使用 12、16、20、24px。
- 图标使用统一线性 SVG，避免字符图标在不同设备上产生差异。
- iPhone 端使用系统字体栈和安全区域变量。

### 3. 移动端适配策略

- 使用 `viewport-fit=cover`，并通过 `env(safe-area-inset-top/bottom)` 处理刘海和 Home Indicator。
- 底部导航固定高度约 84px，并为主内容预留底部空间。
- 记录页关闭会推动表单位置的进入动画。
- 对 320px 小屏单独缩小标题和左右内边距。
- 对 393px～430px 常见 iPhone 宽度使用流式网格，不使用桌面固定宽度缩放。

## 六、代码工作原理

### 页面加载与路由

`index.html` 提供固定外壳：顶部品牌栏、桌面侧栏、主内容容器、底部移动导航和 PWA 元数据。页面通过 URL hash 路由，例如：

- `#dashboard`：首页
- `#new`：新建训练
- `#history`：历史
- `#summary`：汇总
- `#settings`：设置
- `#detail/<id>`：训练详情
- `#edit/<id>`：编辑训练

所有脚本加载完成后，`index.html` 最后调用一次 `render()`。之后监听 `hashchange`，根据 hash 重新绘制 `#app` 中的内容。

### 数据存储

当前是纯前端本地模式，主要数据键为：

```text
fitness-records-v1       训练记录
fitness-profile-v1       身高等个人信息
fitness-weight-v1        体重历史
fitness-workout-drafts-v1 未完成训练草稿
```

训练保存时，`app.js` 会进行日期、时长、部位、动作和组数据校验，再写入 `localStorage`。因此刷新当前设备上的页面不会丢失，但换设备或换浏览器不会自动同步。

### 力量训练与有氧训练

`cardio.js` 在基础应用逻辑之上增加有氧流程：

- `cardioCatalog` 定义每种有氧类型及其字段。
- `cardioModeMarkup()` 生成有氧表单。
- `strengthModeMarkup()` 生成力量训练表单。
- `switchWorkoutType()` 在两种流程之间切换。
- `collectCardioData()` 收集距离、热量等有氧指标。
- `cardioMetric()` 根据时长和距离计算平均配速或速度。

力量训练保持逐动作、逐组的数据结构：

```text
record
└── exercises[]
    └── sets[]
        ├── weight
        ├── reps
        ├── rir
        └── isWarmup
```

### 草稿、反馈和动效

- `draft.js` 监听表单的 `input`、`change`、页面隐藏和离开事件，延迟写入草稿。
- `feedback.js` 为按钮、添加动作和页面切换提供轻量视觉反馈。
- `motion.js` 为普通页面提供淡入和上移动画。
- 记录页被视为连续输入面，特意跳过页面进入动画，避免 iPhone Safari 出现上下弹动。
- `showCompletion()` 负责完成训练后的绿色圆环反馈，并在短暂展示后进入详情页。

### 汇总与折叠布局

- `progress.js` 根据训练数据补充有效组数、动作进步和个人纪录。
- `layout.js` 将设置和汇总中次要模块收纳进折叠区域。
- `summary()` 负责月历、月份统计和周汇总。

### 图标系统

`app.js` 中的 `IOS_ICON_PATHS` 保存本地 SVG 路径，`icon(name)` 统一输出图标节点。这样无需依赖设备本地 SF Symbols，也不会受到字体、emoji 或浏览器差异影响。

## 七、当前边界与后续建议

这些不属于本次 UI 收尾整改，但如果准备给多人长期使用，建议作为下一阶段：

1. 增加账号登录和云端数据库，解决换设备数据同步问题。
2. 在 Cloudflare Pages 之外增加 API 或 Supabase，用于用户隔离、同步和备份。
3. 如果要读取 Apple Watch / HealthKit，需要原生 iOS App 或 Capacitor/React Native 原生桥接；普通网页无法直接读取完整 HealthKit 数据。
4. 将 `app.js` 中早期遗留的重复函数逐步拆分，避免基础函数与 `cardio.js` 的后置覆盖关系继续扩大。
5. 增加真正的单元测试和端到端测试，当前主要依靠浏览器验收和布局审计。
6. 在真实 iPhone 上补做 VoiceOver、动态字体、低电量模式、Safari 私密模式和弱网测试。

## 八、发布前清单

- [x] 首页主要行动明确
- [x] 记录页进入不跳动
- [x] 有氧与力量流程分离
- [x] 草稿可恢复
- [x] 同部位复制训练
- [x] 图标统一为本地 SVG
- [x] iPhone 15 Pro 无横向溢出
- [x] 底部导航不遮挡最后内容
- [x] PWA 图标和 Apple Touch Icon 已配置
- [x] JavaScript 语法与布局审计通过
- [ ] 真实 iPhone 最终验收
- [ ] 云端账号与数据同步
